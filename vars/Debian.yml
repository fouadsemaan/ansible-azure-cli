---
apt_key_url: https://packages.microsoft.com/keys/microsoft.asc
apt_key_keyring: /etc/apt/trusted.gpg.d/microsoft.gpg

microsoft_apt_repo: deb {{microsoft_mirror}} {{ ansible_distribution_release }} main

azure_cli_pkgs:
  - ca-certificates
  - curl
  - apt-transport-https
  - lsb-release
  - gnupg
